# Build output
bin/
obj/

# Rider / VS / VS Code
.vs/
.idea/
.vscode/
*.user
*.suo
*.userosscache
*.sln.docstates

# Claude Code state and project-instruction files (kept locally, not
# distributed in the public repo).
.claude/
CLAUDE.md
handoff.md

# OS
Thumbs.db
.DS_Store

# Steam Workshop (generated on publish, do not commit initially)
About/PublishedFileId.txt

# NuGet
*.nupkg
.nuget/

# Release artifacts (built via PowerShell Compress-Archive when cutting a tag)
*.zip

# NOTE: Assemblies/ is intentionally tracked — the built DLL is the
# distributable artifact that ships with the mod folder.
